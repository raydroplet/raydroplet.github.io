---
layout: post
title: "Building an Asynchronous Web Crawler"
date: 2026-06-07
---

> lead / opening paragraph

<p align="center">
  <img src="{{ 'assets/posts/crawler/graph-StressMinimization.svg' | relative_url }}" width="45%" />
  <img src="{{ 'assets/posts/crawler/graph-FMMMLayout.svg' | relative_url }}" width="45%" />
</p>


Around one year ago I wrote a web crawler in C++. It was a synchrounous one, where you provided a starting URL \<and> it crawled pages up to a specified depth while collecting their relationships. The resulting data was then passed to a graphing library to generate a visual representation of the network.

Over the past year, I've started experimenting with Rust and encountered a language similar in many aspects, but with some novel features, such as the async/await paradigm that are uncommon in C++. A rewritte of my previous crawler seemed fitting: the crate ecosystem and asynchronous model provided the building blocks for a more robust application compared to what I had created before, while allowing me do better familiarize with the novel concepts.

`(~) conclude your introduction while introducing the following section`
`maybe tell how you plan to make an interactive crawler`

## The Old Architecture
---
Before delving into the more advanced concepts, let's take a look at the previous implementation and the possible improvements we can tackle.

<figure style="margin: 0;">
  <video autoplay loop muted playsinline width="100%" style="border-radius: 8px;">
    <source src="{{ 'assets/posts/crawler/cpp_crawler.mp4' | relative_url }}" type="video/mp4">
    Your browser does not support the video tag.
  </video>
<figcaption>A diagram representing the old application. You can see how simple and straightforward it is.</figcaption>
</figure>

As explained before, a URL and depth are given and pages are crawled. The noticeable aspect here is how it is done using a recursive function: you call the `crawl` function for the root url and for all its fetched urls call `crawl` is called again for them, which call for their returned pages and so on. This is handy for the simplicity, assuming you're only interested in small networks.

> rewrite the following paragraph

This process is also _blocking_, we have to wait for the `crawl` function to finish before displaying any results to the user. If the crawler does not print any messages (the ideal scenario since that's the view's work) the program would appear to be frozen until it's done.

> the change seems too abrupt from description to "Given our initial goal..."

Given our initial goal and the idea of making this process more interactive, there are a few improvements we can make:
1. instead of waiting for the final results, we could send the received urls as they come: the rendered graph would gradually increase as pages are processed
2. internally, page fetches and parsing are sequential, we can parallelize that. 
3. originally, the primary interface was just the command line, some graphical UI would be preferable

Let's visualize how those problems could be solved.

### Separation of Concerns
`the last frame the view shouldn't be lit. when the arrow is on '}'`
<figure style="margin: 0;">
  <video autoplay loop muted playsinline width="100%" style="border-radius: 8px;">
    <source src="{{ 'assets/posts/crawler/improved_crawler.mp4' | relative_url }}" type="video/mp4">
    Your browser does not support the video tag.
  </video>
<figcaption>The improved architecture. We use message passing for decoupling.</figcaption>
</figure>


`explanation of the animation` \\
Instead of relying on function call, the view does something different. It sends a message to the crawler with the input information and enters a loop waiting for incoming messages. The crawler receives the message and starts its internal processing. For each page it succesfully crawls, another message is sent to the view with the results. The view receives those messages, updates the graph and renders it.

`explain why its better than before` \\
`proper UI, decoupling` \\
`is the parallelization idea from before still valid? we did not explain it`

## The Actors
---
The previous idea has a well known name, it is called the [actor model](https://en.wikipedia.org/wiki/Actor_model) and the new system will be built based on this premise. Three components are needed for that: a View, a Crawler and a Connector. We will be going through each implementation individually, then combine them to build the final application.

### Crawler

<figure style="margin: 0;">
  <video autoplay loop muted playsinline width="100%" style="border-radius: 8px;">
    <source src="{{ 'assets/posts/crawler/crawler_actor.mp4' | relative_url }}" type="video/mp4">
    Your browser does not support the video tag.
  </video>
<figcaption>High-level overview of the crawler actor internals.</figcaption>
</figure>

We begin by defining the crawler's public interface: incoming commands that dictate its actions, and outgoing responses that report its results. These messages flow through dedicated input and output channels visible externally. Besides commands, we also define events which are internal messages the crawler sends to itself.

The crawler's `event_loop` waits for incoming commands or events and processes them one at a time. Each of those is routed to a specific handler that executes the required logic, potentially emitting new internal events or a final external response. Essentially, complex tasks are broken down by sending messages to itself until the job either succeeds or fails.

<!-- One such example is branching. We start with a request to crawl a page up to a certain depth. The task is executed and a list of contained urls is returned, we then send a `Event::Branch(urls)`. Inside the corresponding handler, `Event::Request` are sent for each url and pages that had already been visited are ommited to avoid duplicates. -->
One such example is branching. We start with a request to crawl a page up to a certain depth. The task is executed and a list of contained urls is returned, we then send a `Event::Branch(urls)`. Inside the corresponding handler, urls for pages that had already been visited are ommited to avoid duplicates and `Event::Request` are sent for each remaining one.

`Event::Error` and `Event::Skipped` may be sent in case a crawl is aborted, caused by an unexpected (network, parse, etc) or expected failure (duplicate pages, non html, etc). When appropriate, external responses are sent for events an external viewer may care about.

From the outside the user only needs to send requests and wait for responses, meanwhile internally the crawler is able to self organize in a way most suitable for processing.

#### Asynchronous Execution
<figure style="margin: 0;">
  <video autoplay loop muted playsinline width="100%" style="border-radius: 8px;">
    <source src="{{ 'assets/posts/crawler/crawler_async.mp4' | relative_url }}" type="video/mp4">
    Your browser does not support the video tag.
  </video>
  <figcaption>
    A high-level overview of how the cooperative multitasking works.
  </figcaption>
</figure>

Because messages are processed one at a time, the `event_loop` must be lightweight and spend a short time handling each one. Time-consuming activities, such as requesting and parsing web pages, are offloaded to Tokio tasks jdone using tokio tasks and asynchronous functions.

Take `Event::Request` as an example: it begins with a guard to check for valid depths, followed by a `tokio::spawn` call that creates a task to execute the provided block of code. Tasks are lightweight, asynchronous units of execution managed by the Tokio runtime, allowing the program to run multiple operations concurrently rather than waiting for one to finish before starting the next. Because `tokio::spawn` is non-blocking, the handler returns immediately, preventing the `event_loop` from stalling.

The await mechanism is critical for managing idle periods. During an operation like an HTTP request, the system spends most of its time waiting for network data to arrive. Instead of blocking the thread, `await` yields control back to the runtime so another task with the pending work can execute. This is a form of cooperative multitasking, where tasks only alternate when they explicitly yield control to others.

#### Parsing

- ...it spawns an independed OS thread and yields. when the thread ends it stops awaiting and continues.

#### Semaphores
`explain why semaphores are used`: so that we don't spawn 483975435 tasks each asking for a http request

For every `Event::Request` a crawling task is spawned. This quantity is not unbounded, there's a limit to it controlled using semaphores.

`introduce the I/O blocking problem`
`introduce async/await and as a solution`
~~However there is a problem:~~ we set a limit to avoid spawning too many tasks and slowing down the system but the http requests itself take time. Frequently they will be waiting, causing a suboptimal \<performance>.

~~`write a preamble for the async/await section`~~
`use a single threaded scenario for simplicity: Consider a single threaded scenario...`

`explain what are blocking operations`
`fancy animations of process_page running concurrently + talk about the semaphore`

### View
`consider removing the last frame as to not spoil the result`
<figure style="margin: 0;">
  <video autoplay loop muted playsinline width="100%" style="border-radius: 8px;">
    <source src="{{ 'assets/posts/crawler/interface.webm' | relative_url }}" type="video/webm">
    Your browser does not support the video tag.
  </video>
  <figcaption>
    The interface evolution over time.
  </figcaption>
</figure>

In the view's perspective, all it has to care about is handling the incoming messages and sending commands out. All other details are handled internally, by the view itself without the rest of the program having to care about it.

I was not able to come up with a fancy interface on the first try and instead had to test different iterations of it, contribuing small improvements each day until it looked good enough. When I was happy with the result I simply stored the responses from the channels and wired the widgets to read from them.

On the left you have information about the crawler: logs of page responses, queues and errors, alongside a total count of those and a average response time for each request. On the right you have a few floating-window-styled widgets organized for node details, graph controls, page hubs and broken links.

The view also features a animated graph of page relationship that can be panned. You can select individual nodes, read the page contents and outgoing links and outgoing links. It is also possible to dynamically request a crawl for nodes not yet visited, featuring the dynamic nature we talked about earlier.

### Connector
<figure>
<img src="{{ 'assets/posts/crawler/blank-image.png' | relative_url }}" alt="fancy motioncanvas static diagram">
<figcaption>diagram with (view), (connector) and (crawler) interconnected using channels. plus a faded DB actor.</figcaption>
</figure>

The connector is a lightweight intermediate used to connect different actors by simply rerouting the messages. While the view and the crawler could share messages directly, having a connector allows for future extensions to the architecture.

One possible improvement is a database: Page responses would be sent both to a database for caching and the view for displaying. If a future request ecounters an already crawled link we could simply have the database query the contents and send them to the view, avoiding unneeded http requests.

#### Sync/Async Communication

As a sidenote, the problem of bridging the synchrounous UI code with the asynchronous tokio actors was also encountered. While actors themselves communicate using native tokio channels, view and connector communication was _highly unergonomic_.

The synchronous view cannot `.await` messages. This forces a choice between freezing the ui with `blocking_recv()`, or continuously polling using `try_recv()`. The problem was solved using flume channels, as they provide both blocking and non-blocking APIs, allowing the most suitable method to be used natively on each side.

Usage of the non-blocking `mpsc::unbounded` was also possible, but that would come at a cost. If messages were not processed fast enough the message queue would grow uncontrolled, a similar behavior to a memory leak. With bounded channels, if the queue space fills up the crawler would simply halt execution until a slot was freed.

## Result (any better header title?)
---

lorem ipsum dolor

<p align="center">
  <img src="{{ 'assets/posts/crawler/interface.png' | relative_url }}" width="100%" />
</p>

- [Github](https://github.com/raydroplet/crawler-rs)

------
------
------
# Random Notes
`remember to revise the final version: clear explanations; avoiding word repetitions`

> suggestion: There are two main ways of architecting a rust async application: message passing and shared state.

`explain the model of how webpages are crawled`
`present concurrency (possible optimization)`
`present I/O blocking (the problem)`
`present async (solution)`

----

`1. animation of the code inside tokio::spawn spawning in a separate box block of code. show 2`
`2. change a scene to a list of boxes and a X number of them being executed at the same time`
`3. explain the execution alternates`
`4. explain blocking I/O and the await, which changes the execution to another box`
`5. show process_page calls other functions which calls other functions all waiting and yielding`
`6. talk about cpu heavy operations like parsing are delegated to a blocking thread pool, explain briefly`
`7. end by showing the task sends a Response::Page on ending, wrap up by relating to the previous high level crawler diagram`

- Scene 1:
  1. centered Event::Request code block
  2. line by line execution
  3. tokio::spawn reached, code block moves left
  4. neon pink square grows from width 0 and moves from the tokio::spawn (x,y) location to the right
  5. execution continues and reaches the end of the Event::Request code block
  6. we repeat the process, now we have two squares
- Scene 2:
  1. a few (6) neon squares aligned, two arrows pointing from below on two (on the center) of them
  2. arrows alternate to indicate which tasks are executing
  3. running squares have outlines of COLOR_X, yielded are COLOR_INACTIVE, waiting are COLOR_Z
  4. zoom in a active box, show a async function executing line by line
  5. we don't start from the top of the function, maybe second or third line before an await
  6. keeps running, we execute a function call inside it with .await, nothing happens
  7. we move to the next line, try again, now the task is yielded
  8. we use the code box outline to represent the state of the task, like done before
- Scene 3:
  1. centered code block `process_page_parse`
  2. code block moves left
  2. line by line execution, we reach the highlighted tokio::spawn_blocking
  3. show a similar animation as before with a square popping up
  4. we await on it and yield the task, the square keeps executing and finishes
  5. we wake up and reach the end of the block of code

- Code:
  - Scene 1:
    ```rust
    ManagerEvent::Request(request) => {
      if request.depth < 0 { continue; }
      tokio::spawn(async {
        let (status, body, urls) =
          process_page(
            permit, request, event_tx).await
        else {
            event.send(Event::Skipped);
            return;
        };
        event_tx.send(Event::Branch(urls));
        let message = PageResult {
            status: status,
            content: body,
            links: urls,
        };
        response.send(
            Response::Page(message)).await;
      });
    }
    ```
  - Scene 2:
    ```rust
    async fn process_page(
    ) -> Option<(StatusCode, String, Vec<Url>)> {
        let _p = permit.acquire_owned().await;

        let (body, status) =
          Self::process_page_request(request).await
        else {
            event_tx.send(Event::Error);
        }
        let urls =
          Self::process_page_parse(request, body.clone).await
        else {
            event_tx.send(Event::Error);
        }
        Some((status, body, urls))
    }
    ```
  - Scene 3:
    ```rust
    async fn process_page_parse(
        url: Url,
        body: String,
    ) -> Result<Vec<Url>, Event> {
        let result =
          tokio::task::spawn_blocking(move || {
            Self::parse_webpage_html(url, body)
        })
        .await;
        return match result {
            Ok(res) => Ok(res),
            Err(err) => Err(Event::Error(err)),
        };
    }
    ```

```rust
ManagerEvent::Request(request) => {
  if request.depth < 0 { continue; }
  tokio::spawn(async {
    let (status, body, urls) =
      process_page(
        permit, request, event_tx).await
    else {
        event.send(Event::Skipped);
        return;
    };
    event_tx.send(Event::Branch(urls));
    let message = PageResult {
        status: status,
        content: body,
        links: urls,
    };
    response.send(
        Response::Page(message)).await;
  });
}
```

